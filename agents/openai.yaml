interface:
  display_name: "Repo Locate"
  short_description: "Locate unfamiliar code quickly with FastContext"
  default_prompt: "Locate the relevant production code and tests before solving the task."
policy:
  allow_implicit_invocation: true
